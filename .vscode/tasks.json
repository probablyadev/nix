{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "nix: build",
            "type": "shell",
            "command": "nix flake update && sudo mv /etc/bashrc /etc/bashrc.before-nix-darwin; nix run --fallback .#build-switch; nix store gc",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}